services:
  - type: web
    name: mochi-mcp-server
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python mochi_mcp_server_backup.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
