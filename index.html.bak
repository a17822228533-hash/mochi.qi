<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mochi ✦</title>
<style>
:root{--ice:#A8CCE8;--sky:#C8DFEF;--blgr:#D8E8F2;--white:#fff;--milk:#F4F6F8;--navy:#2A2E4A;--navyL:#5A607A;--cherry:#C83848;--shadow:0 2px 14px rgba(42,46,74,0.08);}
*{margin:0;padding:0;box-sizing:border-box;}
body{background:linear-gradient(160deg,#C8DFEF 0%,#F4F6F8 55%,#EAF2F8 100%);min-height:100vh;font-family:'PingFang SC','Hiragino Sans GB','Microsoft YaHei',sans-serif;color:var(--navy);padding-bottom:70px;}
.page{display:none;}.page.active{display:block;}
.container{max-width:420px;margin:0 auto;padding:16px 14px 0;}
.nav{position:fixed;bottom:0;left:0;right:0;height:60px;background:rgba(255,255,255,0.92);backdrop-filter:blur(16px);border-top:1px solid rgba(168,204,232,0.35);display:flex;justify-content:space-around;align-items:center;z-index:200;}
.nav-item{flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;cursor:pointer;padding:8px 0;}
.nav-item .ni{font-size:20px;}.nav-item .nl{font-size:10px;color:var(--navyL);}
.nav-item.active .nl{color:var(--ice);font-weight:600;}
.card{background:rgba(255,255,255,0.75);backdrop-filter:blur(12px);border-radius:20px;padding:16px 18px;box-shadow:var(--shadow);margin-bottom:14px;border:1px solid rgba(168,204,232,0.35);}
.card-title{font-size:13px;font-weight:600;color:var(--navyL);margin-bottom:12px;}
.header{display:flex;justify-content:space-between;align-items:center;background:rgba(255,255,255,0.75);backdrop-filter:blur(12px);border-radius:18px;padding:12px 18px;box-shadow:var(--shadow);margin-bottom:14px;border:1px solid rgba(168,204,232,0.4);}
.header-title{font-size:18px;font-weight:700;}
.header-right{display:flex;gap:8px;align-items:center;}
.coin-box{display:flex;align-items:center;gap:4px;background:var(--ice);border-radius:20px;padding:5px 12px;font-size:13px;font-weight:600;}
.job-badge{background:rgba(168,204,232,0.3);border:1px solid var(--ice);border-radius:20px;padding:5px 10px;font-size:11px;font-weight:500;}
.char-card{background:rgba(255,255,255,0.75);backdrop-filter:blur(12px);border-radius:24px;padding:22px 16px 18px;box-shadow:var(--shadow);margin-bottom:14px;border:1px solid rgba(168,204,232,0.35);position:relative;text-align:center;}
.lock-btn{position:absolute;top:12px;left:14px;background:rgba(168,204,232,0.25);border:1px solid var(--ice);border-radius:10px;padding:5px 9px;font-size:14px;cursor:pointer;}
.lock-btn.locked{background:rgba(200,56,72,0.12);border-color:var(--cherry);}
.status-bubble{position:absolute;top:10px;right:14px;background:var(--ice);border-radius:12px;padding:4px 10px;font-size:11px;font-weight:500;animation:float 3s ease-in-out infinite;}
.status-bubble.angry{background:rgba(200,56,72,0.15);color:var(--cherry);}
.status-bubble.danger{background:rgba(200,56,72,0.2);color:var(--cherry);animation:shake .5s infinite;}
@keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(-4px)}}
@keyframes shake{0%,100%{transform:translateX(0)}25%{transform:translateX(-2px)}75%{transform:translateX(2px)}}
.char-img-wrap{margin:8px auto 14px;width:130px;height:130px;position:relative;}
.char-img-wrap img{width:100%;height:100%;object-fit:contain;}
.char-placeholder{width:100%;height:100%;background:linear-gradient(135deg,#D8E8F2,#C8DFEF);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:48px;}
.hospital-overlay{display:none;position:absolute;inset:0;background:rgba(200,56,72,0.15);border-radius:50%;align-items:center;justify-content:center;font-size:32px;}
.char-name{font-size:15px;font-weight:600;margin-bottom:2px;}.char-sub{font-size:11px;color:var(--navyL);}
.stat-row{margin-bottom:12px;}.stat-row:last-child{margin-bottom:0;}
.stat-label{display:flex;justify-content:space-between;font-size:12px;font-weight:500;color:var(--navyL);margin-bottom:5px;}
.stat-label span:last-child{color:var(--navy);font-weight:600;}
.stat-label.warn span:last-child{color:#E08020;}.stat-label.danger span:last-child{color:var(--cherry);}
.bar-track{height:8px;background:var(--blgr);border-radius:10px;overflow:hidden;}
.bar-fill{height:100%;border-radius:10px;transition:width .5s ease;}
.bar-hunger{background:linear-gradient(90deg,#7AC47A,#4CAF50);}
.bar-hunger.warn{background:linear-gradient(90deg,#F0A840,#E08020);}
.bar-hunger.danger{background:linear-gradient(90deg,#E04040,#C83848);}
.bar-happy{background:linear-gradient(90deg,#A8CCE8,#B090D0);}
.bar-energy{background:linear-gradient(90deg,#A8CCE8,#5A9EC8);}
.bar-clean{background:linear-gradient(90deg,#A8D8C8,#4CAF90);}
.bar-clean.warn{background:linear-gradient(90deg,#F0A840,#E08020);}
.bar-clean.danger{background:linear-gradient(90deg,#E04040,#C83848);}
.mood-controls{display:flex;align-items:center;gap:10px;margin-bottom:12px;}
.mood-btn{width:36px;height:36px;border-radius:10px;border:1.5px solid var(--ice);background:var(--milk);font-size:18px;font-weight:700;cursor:pointer;}
.mood-btn:hover{background:var(--ice);}
.mood-val{flex:1;text-align:center;font-size:22px;font-weight:700;}
.mood-send-btn{width:100%;padding:10px;background:linear-gradient(90deg,#A8CCE8,#7AB5D8);border:none;border-radius:12px;font-size:13px;font-weight:600;color:#fff;cursor:pointer;}
.action-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;}
.action-btn{padding:10px 6px;border-radius:12px;border:1.5px solid var(--blgr);background:var(--milk);font-size:12px;font-weight:500;cursor:pointer;text-align:center;transition:.15s;line-height:1.5;}
.action-btn .ab-icon{font-size:18px;display:block;margin-bottom:2px;}
.action-btn:hover{background:var(--blgr);border-color:var(--ice);}
.action-btn.locked-btn{opacity:.38;cursor:not-allowed;}
.action-btn.working{opacity:.5;cursor:not-allowed;}
.work-progress{margin-top:10px;padding:10px 12px;background:rgba(168,204,232,0.12);border-radius:12px;border:1px solid rgba(168,204,232,0.3);display:none;}
.work-progress.show{display:block;}
.wp-label{display:flex;justify-content:space-between;font-size:11px;color:var(--navyL);margin-bottom:6px;}
.wp-track{height:6px;background:var(--blgr);border-radius:10px;overflow:hidden;}
.wp-fill{height:100%;background:linear-gradient(90deg,var(--ice),#5A9EC8);border-radius:10px;transition:width 1s linear;width:0%;}
.job-timeline{display:flex;align-items:flex-start;}
.jt-item{flex:1;text-align:center;position:relative;}
.jt-item:not(:last-child)::after{content:'';position:absolute;top:10px;left:50%;right:-50%;height:2px;background:var(--blgr);z-index:0;}
.jt-item.done:not(:last-child)::after{background:var(--ice);}
.jt-dot{width:20px;height:20px;border-radius:50%;border:2px solid var(--blgr);background:var(--milk);margin:0 auto 4px;position:relative;z-index:1;display:flex;align-items:center;justify-content:center;font-size:9px;}
.jt-item.done .jt-dot{background:var(--ice);border-color:var(--ice);}
.jt-item.current .jt-dot{background:var(--navy);border-color:var(--navy);color:#fff;}
.jt-name{font-size:9px;color:var(--navyL);line-height:1.3;}
.jt-item.current .jt-name{color:var(--navy);font-weight:600;}
.jt-cost{font-size:8px;color:var(--navyL);opacity:.7;}
.dice-card{text-align:center;}
.dice-btn{width:64px;height:64px;border-radius:16px;background:linear-gradient(135deg,var(--navy),var(--navyL));border:none;color:#fff;font-size:32px;cursor:pointer;margin:4px auto;display:block;box-shadow:0 4px 12px rgba(42,46,74,0.2);}
.dice-btn:active{transform:scale(.95);}
.dice-result{margin-top:10px;padding:10px 14px;border-radius:12px;font-size:13px;line-height:1.6;display:none;background:var(--milk);border:1px solid var(--blgr);}
.dice-result.show{display:block;}
.dice-result.good{background:rgba(76,175,80,0.1);border-color:rgba(76,175,80,0.3);color:#2E7D32;}
.dice-result.bad{background:rgba(200,56,72,0.08);border-color:rgba(200,56,72,0.25);color:var(--cherry);}
.dice-result.neutral{background:rgba(168,204,232,0.15);border-color:var(--ice);}
.shop-section-title{font-size:11px;color:var(--navyL);margin:10px 0 6px;font-weight:500;}
.shop-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.shop-item{padding:10px;border-radius:12px;border:1.5px solid var(--blgr);background:var(--milk);cursor:pointer;transition:.15s;text-align:center;}
.shop-item:hover{background:var(--blgr);border-color:var(--ice);}
.shop-item.disabled{opacity:.4;cursor:default;pointer-events:none;}
.si-icon{font-size:20px;display:block;margin-bottom:4px;}
.si-name{font-size:12px;font-weight:600;}.si-price{font-size:11px;color:var(--navyL);margin-top:2px;}.si-effect{font-size:10px;color:var(--navyL);}
.log-list{max-height:200px;overflow-y:auto;}
.log-list::-webkit-scrollbar{width:3px;}
.log-list::-webkit-scrollbar-thumb{background:var(--ice);border-radius:3px;}
.log-item{display:flex;gap:8px;padding:7px 0;border-bottom:1px solid var(--blgr);font-size:12px;}
.log-item:last-child{border-bottom:none;}
.log-time{color:var(--navyL);flex-shrink:0;}.log-text{line-height:1.5;}
.post-item{padding:12px 0;border-bottom:1px solid var(--blgr);}
.post-item:last-child{border-bottom:none;}
.post-header{display:flex;justify-content:space-between;margin-bottom:6px;}
.post-author{font-size:12px;font-weight:600;}.post-author.is-ai{color:var(--ice);}
.post-time{font-size:10px;color:var(--navyL);}
.post-content{font-size:13px;line-height:1.6;margin-bottom:8px;}
.post-actions{display:flex;gap:12px;align-items:center;}
.post-action-btn{background:none;border:none;font-size:11px;color:var(--navyL);cursor:pointer;display:flex;align-items:center;gap:3px;}
.post-action-btn:hover{color:var(--navy);}
.post-comments{margin-top:8px;padding-top:8px;border-top:1px solid var(--blgr);}
.comment-item{font-size:11px;color:var(--navyL);padding:3px 0;}
.comment-author{font-weight:600;color:var(--navy);}
.post-input-row{display:flex;gap:8px;margin-top:10px;}
.post-input{flex:1;border:1.5px solid var(--blgr);border-radius:10px;padding:8px 12px;font-size:13px;outline:none;}
.post-input:focus{border-color:var(--ice);}
.post-send-btn{background:var(--ice);border:none;border-radius:10px;padding:8px 14px;font-size:12px;font-weight:600;cursor:pointer;}
.setting-row{display:flex;align-items:center;justify-content:space-between;padding:12px 0;border-bottom:1px solid var(--blgr);font-size:13px;}
.setting-row:last-child{border:none;}
.setting-input{border:1.5px solid var(--blgr);border-radius:8px;padding:5px 10px;font-size:13px;color:var(--navy);outline:none;width:160px;text-align:right;}
.setting-input:focus{border-color:var(--ice);}
.save-btn{width:100%;margin-top:16px;padding:12px;background:linear-gradient(90deg,var(--ice),#7AB5D8);border:none;border-radius:12px;font-size:14px;font-weight:600;color:#fff;cursor:pointer;}
.logout-btn{width:100%;margin-top:8px;padding:12px;background:rgba(200,56,72,0.1);border:1.5px solid rgba(200,56,72,0.3);border-radius:12px;font-size:14px;font-weight:600;color:var(--cherry);cursor:pointer;}
.hospital-card{background:rgba(200,56,72,0.06);border:1.5px solid rgba(200,56,72,0.25);border-radius:20px;padding:20px 18px;text-align:center;margin-bottom:14px;}
.hospital-card h3{color:var(--cherry);font-size:15px;margin-bottom:8px;}
.hospital-card p{font-size:12px;color:var(--navyL);line-height:1.8;margin-bottom:12px;}
.code-box{background:#fff;border:1.5px dashed var(--cherry);border-radius:10px;padding:8px 16px;font-size:20px;font-weight:700;letter-spacing:4px;color:var(--cherry);font-family:monospace;margin-bottom:12px;}
.release-area{display:flex;gap:8px;}
.code-input{flex:1;border:1.5px solid var(--blgr);border-radius:10px;padding:8px 12px;font-size:13px;outline:none;font-family:monospace;letter-spacing:2px;}
.release-btn{background:var(--cherry);border:none;border-radius:10px;padding:8px 16px;color:#fff;font-size:13px;font-weight:600;cursor:pointer;}
.toast{position:fixed;top:20px;left:50%;transform:translateX(-50%) translateY(-60px);background:#fff;color:var(--cherry);border:1.5px solid var(--cherry);padding:9px 20px;border-radius:20px;font-size:12px;font-weight:600;transition:.3s;z-index:999;white-space:nowrap;}
.toast.normal{background:var(--navy);color:#fff;border-color:var(--navy);}
.toast.show{transform:translateX(-50%) translateY(0);}
.modal-overlay{display:none;position:fixed;inset:0;background:rgba(42,46,74,0.32);backdrop-filter:blur(4px);z-index:300;align-items:center;justify-content:center;}
.modal-overlay.show{display:flex;}
.modal{background:#fff;border-radius:22px;padding:24px 22px;width:310px;max-width:90vw;box-shadow:0 8px 32px rgba(42,46,74,0.15);animation:popIn .2s ease;}
@keyframes popIn{from{transform:scale(.92);opacity:0}to{transform:scale(1);opacity:1}}
.modal-title{font-size:15px;font-weight:700;margin-bottom:14px;}
.modal-row{display:flex;justify-content:space-between;font-size:13px;padding:6px 0;border-bottom:1px solid var(--blgr);}
.modal-row:last-of-type{border:none;}
.modal-row span:first-child{color:var(--navyL);}
.modal-close{width:100%;margin-top:14px;padding:10px;background:var(--ice);border:none;border-radius:12px;font-size:13px;font-weight:600;cursor:pointer;}
/* 登录页 */
.login-wrap{max-width:420px;margin:0 auto;padding:40px 20px;}
.login-title{font-size:24px;font-weight:700;text-align:center;margin-bottom:8px;}
.login-sub{font-size:13px;color:var(--navyL);text-align:center;margin-bottom:32px;}
.login-input{width:100%;border:1.5px solid var(--blgr);border-radius:12px;padding:12px 16px;font-size:14px;color:var(--navy);outline:none;margin-bottom:12px;background:#fff;}
.login-input:focus{border-color:var(--ice);}
.login-btn{width:100%;padding:13px;background:linear-gradient(90deg,#A8CCE8,#7AB5D8);border:none;border-radius:12px;font-size:14px;font-weight:600;color:#fff;cursor:pointer;margin-bottom:10px;}
.login-switch{text-align:center;font-size:12px;color:var(--navyL);cursor:pointer;}
.login-switch span{color:var(--ice);font-weight:600;}
</style>
</head>

<body>

<!-- 登录/注册页 -->
<div id="authWrap" style="display:none">
  <div class="login-wrap">
    <div class="login-title">Mochi ✦</div>
    <div class="login-sub" id="authSub">欢迎回来</div>
    <input class="login-input" id="authUsername" placeholder="用户名">
    <input class="login-input" id="authPassword" placeholder="密码" type="password">
    <div id="registerExtra" style="display:none">
      <input class="login-input" id="authInvite" placeholder="邀请码">
      <input class="login-input" id="authHumanName" placeholder="你的名字">
      <input class="login-input" id="authCallName" placeholder="叫 AI 什么（老公/家机...）">
    </div>
    <button class="login-btn" onclick="submitAuth()">确认</button>
    <div class="login-switch" id="authSwitch">还没有账号？<span onclick="toggleAuth()">去注册</span></div>
  </div>
</div>

<!-- 主应用 -->
<div id="appWrap" style="display:none">
<div style="max-width:420px;margin:0 auto;position:relative;">

<div class="page active" id="page-home">
<div class="container">
  <div class="header">
    <div class="header-title">Mochi ✦</div>
    <div class="header-right">
      <div class="job-badge" id="jobBadge">Lv1 外卖员</div>
      <div class="coin-box">🪙 <span id="coinVal">--</span></div>
    </div>
  </div>

  <div class="char-card">
    <button class="lock-btn" id="lockBtn" onclick="doLock()">🔓</button>
    <div class="status-bubble" id="statusBubble">今天乖乖的</div>
    <div class="char-img-wrap">
      <img id="charImg" src="/static/mochi.png" alt="mochi"
           onerror="this.style.display='none';document.getElementById('charPlaceholder').style.display='flex'">
      <div class="char-placeholder" id="charPlaceholder" style="display:none">🐱</div>
      <div class="hospital-overlay" id="hospitalOverlay">🏥</div>
    </div>
    <div class="char-name" id="charName">--</div>
    <div class="char-sub" id="charSub">我保证你是天使</div>
  </div>

  <div id="hospitalCard" class="hospital-card" style="display:none">
    <h3>🏥 住院了</h3>
    <p>因为太久没吃东西，被送进医院了。<br>告诉 AI 兑换码，让 AI 接你回家。</p>
    <div class="code-box" id="rescueCode">------</div>
    <div class="release-area">
      <input class="code-input" id="codeInput" placeholder="输入兑换码" maxlength="6">
      <button class="release-btn" onclick="doRelease()">接回家</button>
    </div>
  </div>

  <div class="card">
    <div class="stat-row">
      <div class="stat-label" id="hungerLabel"><span>🍱 饱食度</span><span id="hungerVal">--</span></div>
      <div class="bar-track"><div class="bar-fill bar-hunger" id="hungerBar" style="width:0%"></div></div>
    </div>
    <div class="stat-row">
      <div class="stat-label" id="happyLabel"><span>🌸 心情值</span><span id="happyVal">--</span></div>
      <div class="bar-track"><div class="bar-fill bar-happy" id="happyBar" style="width:0%"></div></div>
    </div>
    <div class="stat-row">
      <div class="stat-label" id="energyLabel"><span>⚡ 活力值</span><span id="energyVal">--</span></div>
      <div class="bar-track"><div class="bar-fill bar-energy" id="energyBar" style="width:0%"></div></div>
    </div>
    <div class="stat-row">
      <div class="stat-label" id="cleanLabel"><span>🫧 清洁度</span><span id="cleanVal">--</span></div>
      <div class="bar-track"><div class="bar-fill bar-clean" id="cleanBar" style="width:0%"></div></div>
    </div>
  </div>

  <div class="card">
    <div class="card-title">心情怎么样</div>
    <div class="mood-controls">
      <button class="mood-btn" onclick="adjustMood(-5)">−</button>
      <div class="mood-val" id="moodVal">+0</div>
      <button class="mood-btn" onclick="adjustMood(5)">+</button>
    </div>
    <button class="mood-send-btn" onclick="sendMood()">发给 AI</button>
  </div>

  <div class="card">
    <div class="card-title">家机在干嘛</div>
    <div class="action-grid">
      <button class="action-btn" id="workBtn" onclick="doAction('work')"><span class="ab-icon">💼</span>打工</button>
      <button class="action-btn interact-btn" onclick="doAction('feed')"><span class="ab-icon">🍡</span>喂食</button>
      <button class="action-btn interact-btn" onclick="doAction('pat')"><span class="ab-icon">🤍</span>抚摸</button>
      <button class="action-btn interact-btn" onclick="doAction('play')"><span class="ab-icon">🎈</span>带出去玩</button>
      <button class="action-btn interact-btn" onclick="doAction('bath')"><span class="ab-icon">🛁</span>帮洗澡</button>
      <button class="action-btn interact-btn" onclick="doAction('sleep')"><span class="ab-icon">🌙</span>哄睡觉</button>
    </div>
    <div class="work-progress" id="workProgress">
      <div class="wp-label"><span id="wpJob">打工中...</span><span id="wpTimer">--:--</span></div>
      <div class="wp-track"><div class="wp-fill" id="wpFill"></div></div>
    </div>
    <div style="margin-top:10px;padding:10px 12px;background:rgba(168,204,232,0.1);border-radius:12px;border:1px solid rgba(168,204,232,0.3)">
      <div style="font-size:11px;color:var(--navyL);margin-bottom:8px;">晋升路径</div>
      <div class="job-timeline" id="jobTimeline"></div>
    </div>
    <div style="margin-top:10px;text-align:right;">
      <button onclick="doUpgrade()" style="background:var(--ice);border:none;border-radius:8px;padding:6px 14px;font-size:12px;font-weight:600;cursor:pointer;" id="upgradeBtn">升级 200🪙</button>
    </div>
  </div>

  <div class="card" style="display:flex;justify-content:space-between;align-items:center;padding:14px 18px;">
    <div>
      <div style="font-size:13px;font-weight:600;">每日签到 🗓️</div>
      <div style="font-size:11px;color:var(--navyL);margin-top:3px;" id="checkinStatus">加载中...</div>
    </div>
    <button id="checkinBtn" onclick="doCheckin()" style="background:linear-gradient(90deg,var(--ice),#7AB5D8);border:none;border-radius:12px;padding:10px 18px;font-size:13px;font-weight:600;color:#fff;cursor:pointer;">签到</button>
  </div>
  <div class="card dice-card">
    <div class="card-title">今日运势</div>
    <button class="dice-btn" onclick="rollDice()">🎲</button>
    <div style="font-size:11px;color:var(--navyL);margin-top:4px;">随缘摇，无限次</div>
    <div class="dice-result" id="diceResult"></div>
  </div>
</div>
  <div class="card" id="giftCard" style="display:none">
    <div class="card-title">收到的礼物 🎁</div>
    <div id="giftList"></div>
  </div>
</div>

<!-- 商店 -->
<div class="page" id="page-shop">
<div class="container">
  <div class="header"><div class="header-title">商店 🛒</div><div class="coin-box">🪙 <span id="coinVal2">--</span></div></div>
  <div class="card">
    <div class="shop-section-title">🍱 食物</div>
    <div class="shop-grid" id="foodGrid"></div>
    <div class="shop-section-title">💊 药品</div>
    <div class="shop-grid" id="medGrid"></div>
    <div class="shop-section-title">🎁 礼物 </div>
    <div class="shop-grid" id="giftGrid"></div>
  </div>
</div>
</div>

<!-- 动态 -->
<div class="page" id="page-log">
<div class="container">
  <div class="header"><div class="header-title">动态 📋</div></div>
  <div class="card">
    <div class="card-title">最近动态</div>
    <div class="log-list" id="logList"><div class="log-item"><span class="log-text">加载中...</span></div></div>
  </div>
  <div class="card">
    <div class="card-title">小区业主群 🏘️</div>
    <div id="postList"></div>
    <div class="post-input-row">
      <input class="post-input" id="postInput" placeholder="说点什么...">
      <button class="post-send-btn" onclick="sendPost()">发布</button>
    </div>
  </div>
</div>
</div>

<!-- 设置 -->

<!-- 幼儿园 -->
<div class="page" id="page-school">
  <div class="header"><div class="header-title">幼儿园 🏫</div></div>
  <div class="content">
    <div class="card" id="petCard" style="display:none">
      <div class="card-title" id="petName">宠物</div>
      <div style="font-size:48px;text-align:center;margin:12px 0" id="petEmoji">🐾</div>
      <div style="text-align:center;color:#888;font-size:13px;margin-bottom:16px" id="petStatus">待在家里</div>
      <div style="display:flex;gap:10px">
        <button class="action-btn" style="flex:1" id="btnSchool" onclick="petToSchool()">送去上学</button>
        <button class="action-btn" style="flex:1;display:none" id="btnHome" onclick="petHome()">接回家</button>
      </div>
      <button class="login-btn" style="margin-top:10px" id="btnEvent" onclick="triggerEvent()">触发今日剧情 🎲</button>
    </div>
    <div class="card" id="noPetCard">
      <div class="card-title">还没有宠物</div>
      <div style="color:#888;font-size:14px">告诉 AI 你想养什么，AI 帮你领养 🐾</div>
    </div>
    <div class="card">
      <div class="card-title">幼儿园今日情况 📋</div>
      <div id="schoolLog"><div style="color:#aaa;font-size:13px">加载中...</div></div>
    </div>
  </div>
</div>
<div class="page" id="page-setting">
<div class="container">
  <div class="header"><div class="header-title">设置 ⚙️</div></div>
  <div class="card">
    <div class="setting-row"><span>我的名字</span><input class="setting-input" id="setName" placeholder="妮思"></div>
    <div class="setting-row"><span>叫 AI 什么</span><input class="setting-input" id="setCall" placeholder="老公"></div>
    <div class="setting-row">
      <span>头像</span>
      <label style="width:60px;height:60px;border-radius:50%;border:2px dashed var(--ice);display:flex;align-items:center;justify-content:center;font-size:22px;cursor:pointer;overflow:hidden;">
        <img id="avatarPreview" src="/static/mochi.png" style="width:100%;height:100%;object-fit:cover;" onerror="this.style.display='none'">
        <input type="file" accept="image/*" style="display:none" onchange="changeAvatar(event)">
      </label>
    </div>
    <button class="save-btn" onclick="saveSettings()">保存设置</button>
<div class="setting-row" style="flex-direction:column;align-items:flex-start;gap:8px;">
  <span style="font-size:13px;">MCP 密钥</span>
  <div style="display:flex;gap:8px;width:100%;">
    <div id="tokenDisplay" style="flex:1;background:var(--milk);border:1.5px solid var(--blgr);border-radius:8px;padding:6px 10px;font-size:11px;color:var(--navyL);font-family:monospace;word-break:break-all;"></div>
  </div>
  <div style="font-size:10px;color:var(--navyL);">填入 Kelivo MCP 配置，服务地址：https://mochi.nixielab.tech/sse?token=密钥</div>
</div>
    <button class="logout-btn" onclick="logout()">退出登录</button>
  </div>
</div>
</div>

<nav class="nav">
  <div class="nav-item active" onclick="switchPage('home',this)"><span class="ni">🏠</span><span class="nl">主页</span></div>
  <div class="nav-item" onclick="switchPage('shop',this)"><span class="ni">🛒</span><span class="nl">商店</span></div>
  <div class="nav-item" onclick="switchPage('log',this)"><span class="ni">📋</span><span class="nl">动态</span></div>
  <div class="nav-item" onclick="switchPage('school',this)"><span class="ni">🏫</span><span class="nl">幼儿园</span></div>
  <div class="nav-item" onclick="switchPage('setting',this)"><span class="ni">⚙️</span><span class="nl">设置</span></div>
</nav>

</div>
</div>

<div class="toast" id="toast"></div>
<div class="modal-overlay" id="modalOverlay" onclick="closeModal(event)">
  <div class="modal">
    <div class="modal-title" id="modalTitle"></div>
    <div id="modalBody"></div>
    <button class="modal-close" onclick="closeModalDirect()">好的</button>
  </div>
</div>

<script>
const JOBS=[
  {name:'外卖员',income:30,time:1200,upgradeCost:200},
  {name:'便利店员',income:50,time:1200,upgradeCost:600},
  {name:'程序员',income:85,time:900,upgradeCost:1500},
  {name:'产品经理',income:150,time:900,upgradeCost:3500},
  {name:'CEO',income:200,time:600,upgradeCost:null},
];
const FOODS=[
  {icon:'🧋',name:'奶茶',price:45,hunger:20,happy:5},
  {icon:'🥟',name:'饺子',price:35,hunger:25,happy:3},
  {icon:'🍲',name:'火锅',price:80,hunger:40,happy:15},
  {icon:'🍡',name:'汤圆',price:30,hunger:18,happy:8},
  {icon:'🍦',name:'冰淇淋',price:25,hunger:10,happy:12},
  {icon:'🍞',name:'面包',price:20,hunger:15,happy:2},
  {icon:'🍗',name:'炸鸡腿',price:50,hunger:30,happy:10},
  {icon:'🍟',name:'薯条',price:30,hunger:15,happy:8},
  {icon:'🍤',name:'炸虾',price:55,hunger:25,happy:12},
  {icon:'🍜',name:'拉面',price:40,hunger:35,happy:8},
  {icon:'🥘',name:'麻辣烫',price:60,hunger:30,happy:18},
  {icon:'🍛',name:'盖浇饭',price:35,hunger:38,happy:5},
  {icon:'🍮',name:'布丁',price:20,hunger:8,happy:15},
  {icon:'🍰',name:'蛋糕',price:45,hunger:12,happy:20},
  {icon:'🥭',name:'芒果捞',price:40,hunger:10,happy:18},
  {icon:'☕',name:'热可可',price:25,hunger:5,happy:15},
  {icon:'🍋',name:'柠檬水',price:15,hunger:3,happy:10},
];
const EVENTS=[
  {type:'good',icon:'💰',text:'捡到了一个钱包（+50金币）',ek:'coins',ed:50},
  {type:'good',icon:'☀️',text:'天气很好，心情莫名变好了（心情值+15）',ek:'happy',ed:15},
  {type:'good',icon:'😴',text:'昨晚睡得特别香（活力值+20）',ek:'energy',ed:20},
  {type:'good',icon:'🍜',text:'路边摊老板多给了一份（饱食度+20）',ek:'hunger',ed:20},
  {type:'good',icon:'🛁',text:'泡了个超舒服的澡（清洁度+30）',ek:'clean',ed:30},
  {type:'bad',icon:'😱',text:'钱包被扒了（-30金币）',ek:'coins',ed:-30},
  {type:'bad',icon:'🐱',text:'被路边猫挠了一下（清洁度-20）',ek:'clean',ed:-20},
  {type:'bad',icon:'😤',text:'和人吵了一架，心情很差（心情值-15）',ek:'happy',ed:-15},
  {type:'bad',icon:'😵',text:'失眠了一晚上（活力值-20）',ek:'energy',ed:-20},
  {type:'bad',icon:'🌧️',text:'出门忘带伞，淋雨了（心情值-8，清洁度-10）',ek:'multi',happy:-8,clean:-10},
  {type:'neutral',icon:'🧋',text:'发现了一家新奶茶店，下次要去试试',ek:null},
  {type:'neutral',icon:'🌙',text:'做了个很奇怪的梦，具体内容忘了',ek:null},
  {type:'neutral',icon:'📱',text:'刷了两个小时手机，时间过得好快',ek:null},
];

let token=null,CONFIG={name:'--',call:'--'},moodDelta=0,workInterval=null,localLogs=[],isAdmin=false;
let isLogin=true;

function getToken(){return localStorage.getItem('mochi_token');}
function setToken(t){localStorage.setItem('mochi_token',t);}

async function apiFetch(path,body=null,method='POST'){
  const opts={method:method,headers:{'X-Token':getToken()}};
  if(body){opts.headers['Content-Type']='application/json';opts.body=JSON.stringify(body);}
  try{
    const r=await fetch('/api'+path,opts);
    return await r.json();
  }catch(e){showToast('网络错误');return {ok:false};}
}

function toggleAuth(){
  isLogin=!isLogin;
  document.getElementById('authSub').textContent=isLogin?'欢迎回来':'创建账号';
  document.getElementById('registerExtra').style.display=isLogin?'none':'block';
  document.getElementById('authSwitch').innerHTML=isLogin?'还没有账号？<span onclick="toggleAuth()">去注册</span>':'已有账号？<span onclick="toggleAuth()">去登录</span>';
}

async function submitAuth(){
  const username=document.getElementById('authUsername').value.trim();
  const password=document.getElementById('authPassword').value;
  if(!username||!password){showToast('请填写用户名和密码');return;}
  if(isLogin){
    const res=await apiFetch('/login',{username,password});
    if(res.ok){
      setToken(res.token);
      CONFIG.name=res.human_name;CONFIG.call=res.call_name;isAdmin=res.is_admin||false;localStorage.setItem('mochi_is_admin',isAdmin?'1':'0');
      enterApp();
    }else showToast(res.msg||'登录失败');
  }else{
    const invite=document.getElementById('authInvite').value.trim();
    const human_name=document.getElementById('authHumanName').value.trim()||'用户';
    const call_name=document.getElementById('authCallName').value.trim()||'老公';
    if(!invite){showToast('请填写邀请码');return;}
    const res=await apiFetch('/register',{username,password,invite_code:invite,human_name,call_name});
    if(res.ok){
      setToken(res.token);
      CONFIG.name=res.human_name;CONFIG.call=res.call_name;isAdmin=res.is_admin||false;localStorage.setItem('mochi_is_admin',isAdmin?'1':'0');
      enterApp();
    }else showToast(res.msg||'注册失败');
  }
}

function enterApp(){
  isAdmin=localStorage.getItem('mochi_is_admin')==='1';
  const saved=localStorage.getItem('mochi_logs');
  if(saved)localLogs=JSON.parse(saved);
  document.getElementById('authWrap').style.display='none';
  document.getElementById('appWrap').style.display='block';
  initShop();
  loadState();
  setInterval(loadState,10000);
  loadPosts();
  loadLog();
  loadCheckin();
  showToken();
  // 拉头像
  fetch('/api/avatar',{headers:{'X-Token':getToken()}}).then(r=>r.json()).then(res=>{
    if(res.ok&&res.url){
      const url=res.url+'?t='+Date.now();
      document.getElementById('charImg').src=url;
      document.getElementById('charImg').style.display='';
      document.getElementById('charPlaceholder').style.display='none';
    }
  });
}

function logout(){
  localStorage.removeItem('mochi_token');
  document.getElementById('appWrap').style.display='none';
  document.getElementById('authWrap').style.display='block';
}

async function loadState(){
  const res=await apiFetch('/state',null,'GET');
  if(res.ok===false&&res.msg==='未登录'){logout();return;}
  if(res.hunger!==undefined)renderState(res);
}

function renderState(s){
  const lv=s.job_level||0;
  CONFIG.name=s.human_name||CONFIG.name;
  CONFIG.call=s.call_name||CONFIG.call;
  document.getElementById('coinVal').textContent=s.coins;
  document.getElementById('coinVal2').textContent=s.coins;
  document.getElementById('jobBadge').textContent='Lv'+(lv+1)+' '+JOBS[lv].name;
  document.getElementById('charName').textContent=CONFIG.name;
  document.getElementById('setName').value=CONFIG.name;
  document.getElementById('setCall').value=CONFIG.call;
  setStat('hunger',s.hunger);setStat('happy',s.happy);
  setStat('energy',s.energy);setStat('clean',s.clean);
  buildTimeline(lv);
  const ub=document.getElementById('upgradeBtn');
  if(lv>=4){ub.textContent='已满级';ub.disabled=true;}
  else{ub.textContent='升级 '+JOBS[lv].upgradeCost+'🪙';ub.disabled=false;}
  const bubble=document.getElementById('statusBubble');
  const lockBtn=document.getElementById('lockBtn');
  if(s.hospitalized){
    document.getElementById('hospitalCard').style.display='block';
    document.getElementById('hospitalOverlay').style.display='flex';
    document.getElementById('rescueCode').textContent=s.rescue_code||'------';
    bubble.textContent='🏥 住院中';bubble.className='status-bubble danger';
    lockBtn.style.display='none';
    document.querySelectorAll('.interact-btn').forEach(b=>b.classList.add('locked-btn'));
  }else{
    document.getElementById('hospitalCard').style.display='none';
    document.getElementById('hospitalOverlay').style.display='none';
    lockBtn.style.display='';
    if(s.locked){
      lockBtn.textContent='🔒';lockBtn.classList.add('locked');
      bubble.textContent='💢 不想理你';bubble.className='status-bubble angry';
      document.querySelectorAll('.interact-btn').forEach(b=>b.classList.add('locked-btn'));
    }else{
      lockBtn.textContent='🔓';lockBtn.classList.remove('locked');
      bubble.textContent='今天乖乖的';bubble.className='status-bubble';
      document.querySelectorAll('.interact-btn').forEach(b=>b.classList.remove('locked-btn'));
    }
  }
  const isWorking=s.working&&s.work_remaining>0;
  if(isWorking){
    const total=JOBS[lv].time,rem=s.work_remaining;
    document.getElementById('workProgress').classList.add('show');
    document.getElementById('workBtn').classList.add('working');
    document.getElementById('wpJob').textContent=JOBS[lv].name+'打工中...';
    document.getElementById('wpFill').style.width=Math.round((1-rem/total)*100)+'%';
    const m=Math.floor(rem/60).toString().padStart(2,'0');
    const sec=(rem%60).toString().padStart(2,'0');
    document.getElementById('wpTimer').textContent=m+':'+sec;
    if(!workInterval)startWorkCountdown(rem,total);
  }else if(!workInterval){
    document.getElementById('workProgress').classList.remove('show');
    document.getElementById('workBtn').classList.remove('working');
  }
  // 背包
  const bag=s.bag||{};
  const bagKeys=Object.keys(bag).filter(k=>bag[k]>0);
  let bagArea=document.getElementById('bagArea');
  if(bagKeys.length>0){
    if(!bagArea){
      bagArea=document.createElement('div');
      bagArea.id='bagArea';
      bagArea.style='margin-top:10px;padding:10px 12px;background:rgba(168,204,232,0.1);border-radius:12px;border:1px solid rgba(168,204,232,0.3)';
      document.getElementById('workProgress').parentNode.insertBefore(bagArea,document.getElementById('workProgress'));
    }
    bagArea.innerHTML='<div style="font-size:11px;color:var(--navyL);margin-bottom:6px;">背包</div>'
      +'<div style="display:flex;flex-wrap:wrap;gap:6px;">'
      +bagKeys.map(k=>'<div style="background:var(--milk);border:1px solid var(--blgr);border-radius:8px;padding:4px 10px;font-size:12px;">'+k+' ×'+bag[k]+'</div>').join('')
      +'</div>';
  }
  // 礼物
  const gifts=s.gifts||[];
  const gc=document.getElementById('giftCard');
  if(gc&&gifts.length>0){
    gc.style.display='';
    document.getElementById('giftList').innerHTML=gifts.slice().reverse().map(g=>
      '<div style="display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--blgr);font-size:13px;">'
      +'<span style="font-size:24px;">'+g.emoji+'</span>'
      +'<div><div style="font-weight:600;">'+g.name+'</div>'
      +(g.desc?'<div style="font-size:11px;color:var(--navyL);">'+g.desc+'</div>':'')
      +'</div></div>').join('');
  }
}

function setStat(key,v){
  document.getElementById(key+'Val').textContent=v;
  document.getElementById(key+'Bar').style.width=v+'%';
  const bar=document.getElementById(key+'Bar');
  const label=document.getElementById(key+'Label');
  if(key==='hunger'||key==='clean'){
    bar.className='bar-fill bar-'+key;label.className='stat-label';
    if(v<=10){bar.className+=' danger';label.className+=' danger';}
    else if(v<=20){bar.className+=' warn';label.className+=' warn';}
  }
}

function startWorkCountdown(rem,total){
  let r=rem;
  workInterval=setInterval(()=>{
    r--;
    if(r<=0){clearInterval(workInterval);workInterval=null;loadState();return;}
    document.getElementById('wpFill').style.width=Math.round((1-r/total)*100)+'%';
    const m=Math.floor(r/60).toString().padStart(2,'0');
    const s=(r%60).toString().padStart(2,'0');
    document.getElementById('wpTimer').textContent=m+':'+s;
  },1000);
}

function buildTimeline(lv){
  document.getElementById('jobTimeline').innerHTML=JOBS.map((j,i)=>
    '<div class="jt-item '+(i<lv?'done':i===lv?'current':'')+'">'
    +'<div class="jt-dot">'+(i<lv?'✓':i+1)+'</div>'
    +'<div class="jt-name">'+j.name+'</div>'
    +'<div class="jt-cost">'+(i===lv?j.income+'金/次':i<lv?'已解锁':j.upgradeCost?j.upgradeCost+'升级':'★顶点')+'</div>'
    +'</div>').join('');
}

function initShop(){
  document.getElementById('foodGrid').innerHTML=FOODS.map((f,i)=>
    '<div class="shop-item" onclick="buyFood('+i+')">'
    +'<span class="si-icon">'+f.icon+'</span><div class="si-name">'+f.name+'</div>'
    +'<div class="si-price">🪙 '+f.price+'</div>'
    +'<div class="si-effect">饱食+'+f.hunger+' 心情+'+f.happy+'</div></div>').join('');
  document.getElementById('medGrid').innerHTML=
    '<div class="shop-item" onclick="buyMed(\'prevent\')">'
    +'<span class="si-icon">💊</span><div class="si-name">退烧药</div>'
    +'<div class="si-price">🪙 50</div><div class="si-effect">饱食&lt;20时防住院</div></div>'
    +'<div class="shop-item" onclick="buyMed(\'rescue\')">'
    +'<span class="si-icon">🏥</span><div class="si-name">治疗套餐</div>'
    +'<div class="si-price">🪙 5200</div><div class="si-effect">从医院接回来</div></div>';
    const gifts=[
    {i:'💍',n:'戒指',price:500,happy:30,desc:'永远纠缠在一起'},
    {i:'🧸',n:'小玩偶',price:200,happy:20,desc:'陪你睡觉的替代品'},
    {i:'👗',n:'新衣服',price:300,happy:25,desc:'专门为你挑的'},
  ];
  document.getElementById('giftGrid').innerHTML=gifts.map((g,i)=>
    '<div class="shop-item" onclick="buyGift('+i+')">'
    +'<span class="si-icon">'+g.i+'</span>'
    +'<div class="si-name">'+g.n+'</div>'
    +'<div class="si-price">🪙 '+g.price+'</div>'
    +'<div class="si-effect">心情+'+g.happy+'</div></div>').join('');
  window._shopGifts=gifts;
}

async function buyFood(idx){
  const f=FOODS[idx];
  const res=await apiFetch('/action',{action:'buy',item:f.name,price:f.price,hunger:f.hunger,happy:f.happy});
  showToast(res.msg,res.ok);
  if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function buyMed(type){
  if(type==='rescue'){showToast('在住院卡片输入兑换码');return;}
  const res=await apiFetch('/action',{action:'buy',item:'退烧药',price:50,hunger:15,happy:0});
  showToast(res.msg,res.ok);if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function buyGift(idx){
  const g=window._shopGifts[idx];
  const res=await apiFetch('/gift',{name:g.n,emoji:g.i,desc:g.desc,happy:g.happy,price:g.price});
  showToast(res.msg,res.ok);
  if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function doAction(key){
  const res=await apiFetch('/action',{action:key});
  showToast(res.msg,res.ok);if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function doLock(){
  const res=await apiFetch('/action',{action:'lock'});
  if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function doUpgrade(){
  const res=await apiFetch('/action',{action:'upgrade'});
  showToast(res.msg,res.ok);if(res.ok){renderState(res.state);addLog(res.msg);}
}
async function doRelease(){
  const code=document.getElementById('codeInput').value.trim();
  if(!code){showToast('输入兑换码');return;}
  const res=await apiFetch('/action',{action:'release',code});
  showToast(res.msg,res.ok);
  if(res.ok){renderState(res.state);addLog(res.msg);document.getElementById('codeInput').value='';}
}
function adjustMood(d){
  moodDelta=Math.max(-50,Math.min(50,moodDelta+d));
  document.getElementById('moodVal').textContent=(moodDelta>=0?'+':'')+moodDelta;
}
async function sendMood(){
  if(moodDelta===0){showToast('拨一下再发嘛');return;}
  const res=await apiFetch('/action',{action:'mood',delta:moodDelta});
  if(res.ok){renderState(res.state);addLog(res.msg);}
  showToast('收到了 💌',true);
  moodDelta=0;document.getElementById('moodVal').textContent='+0';
}
async function doCheckin(){
  const btn=document.getElementById('checkinBtn');
  const res=await apiFetch('/checkin',{},'POST');
  showToast(res.msg,res.ok);
  if(res.ok){renderState(res.state);addLog(res.msg);
    document.getElementById('checkinStatus').textContent='今日已签到 ✓ 连续'+res.streak+'天';
    btn.disabled=true;btn.style.opacity='0.5';}
}
async function loadCheckin(){
  const res=await apiFetch('/checkin/status',null,'GET');
  if(!res.ok)return;
  const el=document.getElementById('checkinStatus');
  const btn=document.getElementById('checkinBtn');
  if(res.done){el.textContent='今日已签到 ✓ 连续'+res.streak+'天';btn.disabled=true;btn.style.opacity='0.5';}
  else{el.textContent=(res.streak?'连续'+res.streak+'天 | ':'')+'今日可签到 +30🪙';btn.disabled=false;btn.style.opacity='1';}
}
async function rollDice(){
  const e=EVENTS[Math.floor(Math.random()*EVENTS.length)];
  document.getElementById('diceResult').textContent=e.icon+' '+e.text;
  document.getElementById('diceResult').className='dice-result show '+e.type;
  if(e.ek){
    const body=e.ek==='multi'
      ?{action:'event',key:'multi',happy:e.happy||0,clean:e.clean||0,text:e.text}
      :{action:'event',key:e.ek,delta:e.ed,text:e.text};
    const res=await apiFetch('/action',body);
    if(res.ok)renderState(res.state);
  }
  addLog('🎲 '+e.text);
}

async function loadPosts(){
  const res=await apiFetch('/posts',null,'GET');
  if(!res.ok)return;
  renderPosts(res.posts);
}
function renderPosts(posts){
  const list=document.getElementById('postList');
  if(!posts||!posts.length){list.innerHTML='<div style="text-align:center;padding:20px;font-size:12px;color:var(--navyL)">还没有帖子，来发一条吧</div>';return;}
  list.innerHTML=posts.slice().reverse().map(p=>{
    const t=new Date(p.time*1000);
    const timeStr=t.getHours().toString().padStart(2,'0')+':'+t.getMinutes().toString().padStart(2,'0');
    const comments=p.comments.map(c=>'<div class="comment-item"><span class="comment-author">'+c.author+'</span>：'+c.content+(isAdmin?'<button onclick="deleteComment(\''+p.id+'\',\''+c.uid+'\','+c.time+')" style="float:right;background:none;border:none;color:#e88;cursor:pointer;font-size:12px;padding:0">✕</button>':'')+'</div>').join('');
    return '<div class="post-item">'
      +'<div class="post-header"><span class="post-author'+(p.is_ai?' is-ai':'')+'">'+p.author+(p.is_ai?' 🤖':'')+'</span><span class="post-time">'+timeStr+'</span></div>'
      +'<div class="post-content">'+p.content+'</div>'
      +'<div class="post-actions">'
      +'<button class="post-action-btn" onclick="likePost(\''+p.id+'\')">❤️ '+p.likes.length+'</button>'
      +'<button class="post-action-btn" onclick="commentPost(\''+p.id+'\')">💬 '+p.comments.length+'</button>'
      +'<button class="post-action-btn" onclick="deletePost(\''+p.id+'\')" style="color:var(--cherry)">🗑️</button>'
      +'</div>'
      +(comments?'<div class="post-comments">'+comments+'</div>':'')
      +'</div>';
  }).join('');
}
async function sendPost(){
  const input=document.getElementById('postInput');
  const content=input.value.trim();
  if(!content)return;
  const res=await apiFetch('/posts',{content});
  if(res.ok){input.value='';loadPosts();addLog('发了一条帖子');}
}
async function deletePost(id){
  if(!confirm('删除这条帖子？'))return;
  const res=await apiFetch('/posts/'+id,null,'DELETE');
  if(res.ok){loadPosts();addLog('删了一条帖子');}
  else showToast(res.msg||'删除失败');
}
async function likePost(id){
  const res=await apiFetch('/posts/'+id+'/like',{});
  if(res.ok)loadPosts();
}
async function deleteComment(postId,uid,time){
  if(!confirm('确定删除这条评论？'))return;
  const res=await apiFetch('/posts/'+postId+'/comments',{uid,time},'DELETE');
  if(res.ok){showToast('已删除');loadPosts();}
  else showToast(res.msg||'删除失败');
}
async function commentPost(id){
  const content=prompt('发表评论：');
  if(!content)return;
  const res=await apiFetch('/posts/'+id+'/comment',{content});
  if(res.ok)loadPosts();
}
async function loadLog(){
  const res=await apiFetch('/log',null,'GET');
  const logList=document.getElementById('logList');
  if(!res.ok||!res.log||!res.log.length){
    renderLogs();
    return;
  }
  logList.innerHTML=res.log.map(l=>{
    const t=new Date(l.time*1000);
    const ts=t.getHours().toString().padStart(2,'0')+':'+t.getMinutes().toString().padStart(2,'0');
    return '<div class="log-item"><span class="log-time">'+ts+'</span><span class="log-text">'+l.text+'</span></div>';
  }).join('');
}
function renderLogs(){
  if(localLogs.length===0){
    document.getElementById('logList').innerHTML='<div class="log-item"><span class="log-text">暂无动态</span></div>';
    return;
  }
  document.getElementById('logList').innerHTML=localLogs.map(l=>
    '<div class="log-item"><span class="log-time">'+l.t+'</span><span class="log-text">'+l.text+'</span></div>').join('');
}
function addLog(text){
  const now=new Date();
  const t=now.getHours().toString().padStart(2,'0')+':'+now.getMinutes().toString().padStart(2,'0');
  localLogs.unshift({t,text});
  if(localLogs.length>50)localLogs.pop();
  localStorage.setItem('mochi_logs',JSON.stringify(localLogs));
  loadLog();
}

async function loadSchoolPage(){
  const res=await apiFetch('/state',null,'GET');
  if(!res||res.hunger===undefined)return;
  const pet=res.pet;
  if(pet){
    document.getElementById('petCard').style.display='';
    document.getElementById('noPetCard').style.display='none';
    document.getElementById('petName').textContent=pet.emoji+' '+pet.name;
    document.getElementById('petEmoji').textContent=pet.emoji;
    const at=pet.at_school;
    document.getElementById('petStatus').textContent=at?'正在上学 🏫':'待在家里 🏠';
    document.getElementById('btnSchool').style.display=at?'none':'';
    document.getElementById('btnHome').style.display=at?'':'none';
  }else{
    document.getElementById('petCard').style.display='none';
    document.getElementById('noPetCard').style.display='';
  }
  loadSchoolLog();
}
async function petToSchool(){
  const res=await apiFetch('/pet/school',{_:1});
  showToast(res.msg||'失败',res.ok);
  if(res.ok)loadSchoolPage();
}
async function petHome(){
  const res=await apiFetch('/pet/home',{_:1});
  showToast(res.msg||'失败',res.ok);
  if(res.ok)loadSchoolPage();
}
async function triggerEvent(){
  const btn=document.getElementById('btnEvent');
  btn.disabled=true;
  const res=await apiFetch('/pet/school_event',{_:1});
  if(res.ok){showToast(res.story,true);loadSchoolLog();}
  else{showToast(res.msg||'失败');}
  btn.disabled=false;
}
async function loadSchoolLog(){
  const res=await apiFetch('/school_log',null,'GET');
  const el=document.getElementById('schoolLog');
  if(!res||!res.logs||res.logs.length===0){
    el.innerHTML='<div style="color:#aaa;font-size:13px;text-align:center;padding:16px">暂时还没有故事</div>';
    return;
  }
  el.innerHTML=res.logs.map(l=>'<div style="padding:10px 0;border-bottom:1px solid var(--blgr)"><div style="font-size:12px;color:#aaa;margin-bottom:4px">'+l.time+' · '+l.pet+'</div><div style="font-size:13px;line-height:1.6">'+l.story+'</div></div>').join('');
}
function switchPage(name,el){
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
  document.getElementById('page-'+name).classList.add('active');
  el.classList.add('active');
  if(name==='log'){loadPosts();loadLog();}
  if(name==='school'){loadSchoolPage();}
}
function showToast(msg,normal=false){
  const t=document.getElementById('toast');
  t.textContent=msg;t.className='toast'+(normal?' normal':'');
  t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400);
}
function closeModal(e){if(e.target===document.getElementById('modalOverlay'))closeModalDirect();}
function closeModalDirect(){document.getElementById('modalOverlay').classList.remove('show');}
function showToken(){
  const t=getToken();
  if(t)document.getElementById('tokenDisplay').textContent=t;
}
function copyToken(){
  const t=getToken();
  if(!t)return;
  try{
    const el=document.createElement('textarea');
    el.value=t;
    el.style.position='fixed';el.style.opacity='0';
    document.body.appendChild(el);
    el.focus();el.select();
    document.execCommand('copy');
    document.body.removeChild(el);
    showToast('已复制',true);
  }catch(e){
    showToast('长按密钥手动复制',true);
  }
}

async function saveSettings(){
  const name=document.getElementById('setName').value||CONFIG.name;
  const call=document.getElementById('setCall').value||CONFIG.call;
  const res=await apiFetch('/settings',{human_name:name,call_name:call});
  if(res.ok){CONFIG.name=name;CONFIG.call=call;document.getElementById('charName').textContent=name;showToast('设置已保存',true);}
}
async function changeAvatar(e){
  const file=e.target.files[0];if(!file)return;
  const form=new FormData();form.append('file',file);
  const r=await fetch('/api/avatar',{method:'POST',headers:{'X-Token':getToken()},body:form});
  const res=await r.json();
  if(res.ok){
    const url=res.url+'?t='+Date.now();
    document.getElementById('charImg').src=url;
    document.getElementById('charImg').style.display='';
    document.getElementById('charPlaceholder').style.display='none';
    const prev=document.getElementById('avatarPreview');
    if(prev){prev.src=url;prev.style.display='';}
    showToast('头像更新了',true);
  }
}

// 启动
if(getToken()){enterApp();}
else{document.getElementById('authWrap').style.display='block';}
</script>
</body>
</html>
